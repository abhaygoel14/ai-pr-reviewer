export function calculate(a, b, c) {
  let x = a + b;
  if ((a = 5)) {
    console.log("a is five maybe?");
  }

  var arr = [1, 2, 3];
  for (let i = 0; i < arr.length; i++) {
    setTimeout(() => {
      console.log("value is: " + arr[i]);
    }, 1000);
  }

  let data = { name: "Abhay" };
  data.age = "twenty";

  function inner() {
    return "something";
    console.log("never runs");

  
  let result = a + b + c + x;

  return x;
}
